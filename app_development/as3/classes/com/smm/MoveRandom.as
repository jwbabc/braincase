// $Id: moveRandom.as 443 2008-07-24 18:16:35Z jback $package org.smm {	import flash.display;		public class MoveRandom {		private var logo_x:Number;		private var logo_y:Number;		private var max_width:Number;		private var max_height:Number;		private var currentframe:Number;		private var totalframes:Number;				public function MoveRandom (myMovieClip:MovieClip):void {			logo_x = myMovieClip.x;			logo_y = myMovieClip.y;			max_width = stage.width;			max_height = stage.height;			currentframe = myMovieClip.currentFrame;			totalframes = myMovieClip.totalFrames;		}				// Places logo onto stage at random location within limits.		function moveMe ():void {			if (logo1_mc.currentFrame == logo1_mc.totalframes) {				var logo_x:Number = Math.random() *stage.width;				var logo_y:Number = Math.random() *stage.height;							// Offset the left side for the width of the logo				if (logo_x <= 0) {					logo_x = 0;				}							// Offest the right side for the width of the logo                                                          				if ((logo_x + logo1_mc.width) > stage.width) {					logo_x = (stage.width - logo1_mc.width);					trace (logo1_mc.width);				}							// Offset the top of the logo                                                          				if (logo_y <= 0) {					logo_y = 0;				}							// Offset the botom of the logo                                                          				if ((logo_y + logo1_mc.height) > stage.height) {					logo_y = (stage.height - logo1_mc.height);					trace (logo1_mc.height);				}							logo1_mc.x = logo_x;				logo1_mc.y = logo_y;			}		}	}}
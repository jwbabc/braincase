package {		public class Points {				public static function createPoints(row:Number, col:Number, w:Number, h:Number, sw:Number = 0, sh:Number = 0):Array {						// The last two parameters are offset values, to start the points at a value other than 0						var _points	:Array	= new Array();			var d		:Number	= 0;						for (var j:Number=0; j<row; j++) {				for (var i:Number = 0; i<col; i++) {					_points[d] = new GridPoint(sw+(i*w), sh+(j*h), d);					d++;				}			}						return _points;		}			}}
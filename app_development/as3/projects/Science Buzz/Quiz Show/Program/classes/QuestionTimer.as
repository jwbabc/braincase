// $Id: questionTimer.as 443 2008-07-24 18:16:35Z jback $package {  public class QuestionTimer {		import flash.events.TimerEvent;		import flash.utils.Timer;		// The Timer that acts like a heartbeat.		private var ticker:Timer;		private var n:Number;		private var seconds:Number;				public function QuestionTimer (myCountdownNum):void {			ticker = new Timer(1000);			seconds = myCountdownNum;		}				public function initTimer ():void {			// designates the onTick() method to handle Timer events			ticker.addEventListener (TimerEvent.TIMER, onTick);			// starts the ticking			ticker.start ();		}				public function unInitTimer (myCountdownNum):void {			// stops the ticking			ticker.reset ();			ticker.removeEventListener (TimerEvent.TIMER, onTick);			seconds = myCountdownNum;		}				public function onTick (event:TimerEvent):void {			// updates the clock display			if (seconds >= 0)			{				seconds = seconds - 1;			}		}				// Property access		public function get p_seconds ():Number {			return seconds;		}				public function set p_seconds (setValue:Number):void {			seconds = setValue;		}	}}